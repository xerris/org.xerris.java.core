package org.xerris.core.validation.validators;

import org.xerris.core.validation.Validate;

public class LongValidator extends BaseValidator<Long> {
    public LongValidator(Validate validate) {
        super(validate);
    }
}
