package org.xerris.core.validation.validators;

import org.xerris.core.validation.Validate;

public class IntegerValidator extends BaseValidator<Integer> {
    public IntegerValidator(Validate validate) {
        super(validate);
    }
}

