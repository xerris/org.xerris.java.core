package org.xerris.core.validation.validators;

import org.xerris.core.validation.Validate;

public class DoubleValidator extends BaseValidator<Double> {
    public DoubleValidator(Validate validate) {
        super(validate);
    }
}
